// Common filter and searchable fields
export const excludedFields = ["searchTerm", "sort", "fields", "page", "limit"];

// Searchable fields for tour
export const tourSearchableFields = ["title", "description", "location"];

// Searchable fields for division
export const divisionSearchableFields = ["name", "description"];
